# Copy these values into a local .env file or the Render environment settings.
DATABASE_URL=postgresql://postgres.PROJECT_REF:PASSWORD@POOLER_HOST:6543/postgres?sslmode=require
JWT_SECRET_KEY=replace-with-a-long-random-secret
CORS_ORIGINS=http://localhost:3000
APP_PUBLIC_URL=https://www.infinit-audit.co.uk

# Outbound email. Credentials can be added after the code is deployed.
# For Namecheap Private Email, use the SMTP settings supplied with the mailbox.
SMTP_HOST=mail.privateemail.com
SMTP_PORT=465
SMTP_USERNAME=info@infinit-audit.co.uk
SMTP_PASSWORD=replace-with-mailbox-password
SMTP_FROM_EMAIL=info@infinit-audit.co.uk
SMTP_FROM_NAME=Infinit Audit
SMTP_USE_SSL=true
SMTP_USE_STARTTLS=false
SMTP_TIMEOUT_SECONDS=15

# Scheduled audit reminder worker. The in-process loop is best-effort while the
# web service is awake; INTERNAL_JOB_SECRET also protects an endpoint that can
# be called later by an external scheduled job for guaranteed execution.
REMINDER_LOOP_ENABLED=true
REMINDER_CHECK_SECONDS=900
INTERNAL_JOB_SECRET=replace-with-a-long-random-secret

# Production-paperwork import. The key enables scan/PDF extraction into
# unpublished drafts. gpt-4.1 can accept both images and PDF file inputs.
OPENAI_API_KEY=replace-with-openai-api-key
OPENAI_DOCUMENT_MODEL=gpt-4.1

# Optional S3-compatible storage for protected source scans. If no bucket is
# configured, scans are kept in a separate application-database collection.
PAPERWORK_STORAGE_BUCKET=
PAPERWORK_STORAGE_REGION=eu-west-2
PAPERWORK_STORAGE_ENDPOINT=
PAPERWORK_STORAGE_ACCESS_KEY=
PAPERWORK_STORAGE_SECRET_KEY=

# Optional: creates the first system administrator only when it does not exist.
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=replace-before-deploying

# Only required while running migrate_from_mongodb.py
MONGO_URL=mongodb+srv://user:password@cluster.example.mongodb.net
DB_NAME=infinit_audit
